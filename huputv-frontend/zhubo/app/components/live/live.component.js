import template from './live.html';
import controller from './live.controller';
import './live.scss';

let Component = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default Component;
