import template from './index.html';
import controller from './index.controller';
import './index.scss';

let Component = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default Component;
